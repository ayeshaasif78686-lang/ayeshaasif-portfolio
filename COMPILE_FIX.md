# Compile fix

Fixed Flutter web compile error:
- Added `package:flutter/gestures.dart` so `PointerHoverEvent` used by the magnetic CTA MouseRegion callback resolves correctly.

No design/theme/avatar/content changes were made in this patch.
